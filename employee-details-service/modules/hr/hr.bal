// Define configurable variables, including the HR endpoint
public configurable string hrEndpoint = ?;

// Defaultable configurable — has a default value, so not required
public configurable int hrRequestTimeout = 30;
